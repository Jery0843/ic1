import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Enable standalone output to produce a minimal deployable bundle in .next/standalone
  output: "standalone",
};

export default nextConfig;
